// export const addItemToCollections = (collections, itemToAdd) => {
//   for (let i = 0; i < collections.length; i++) {
//     if (collections[i].mal_id === itemToAdd.mal_id) {
//       return collections;
//     }
//   }

//   return [...collections, { ...itemToAdd }];
// };

// export const removeItemFromCollections = (collections, itemToRemove) => {
//   return collections.filter((item) => item.mal_id !== itemToRemove.mal_id);
// };
